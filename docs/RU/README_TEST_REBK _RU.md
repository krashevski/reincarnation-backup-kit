# ✅ Правильный порядок локального тестирования REBK

[🇬🇧 English](../EN/README_TEST_REBK EN.md) | [🇷🇺 Русский](README_TEST_REBK_RU.md)

## 1️⃣ Клонировать репозиторий заново (чистая проверка)

Важно: не из текущей рабочей копии.
```bash
cd ~
git clone https://github.com/<username>/backup-kit.git rebk-test
cd rebk-test
```

## 2️⃣ Проверить, что версия действительно последняя

```bash
git log --oneline -5
```

Сравни:
- последний коммит
- дату
- сообщение (images: add corrected Ubuntu screenshot… и т.п.)

## 3️⃣ Проверить структуру проекта

```bash
tree -L 2
```

Обрати внимание:
* scripts/
* scripts/i18n/
* menu.sh
* install.sh
* отсутствие TEST_FORCE.md ✅

## 4️⃣ Проверить права на выполнение

```bash
ls -l scripts/*.sh menu.sh install.sh
```

Если нужно:
```bash
chmod +x install.sh menu.sh scripts/*.sh
```

## 5️⃣ Запустить без sudo (важно)

```bash
./scripts/menu.sh
```

Проверь:
- русский / английский язык
- отсутствие link_xxx
- корректные сообщения
- отсутствие ошибок MSG, L, say

## 6️⃣ Проверить show-system-mounts.sh

```bash
scripts/show-system-mounts.sh
```

Должно:
- показывать диски
- монтирования
- симлинки в $HOME
- crontab (или empty)

## 7️⃣ Проверить idempotency (ключевой момент)

Запусти два раза подряд:
```bash
scripts/setup-symlinks.sh
scripts/setup-symlinks.sh
```

Ожидаемо:
- cсылка корректна
- ничего не ломается
- никаких ln: отказано в доступе

## 8️⃣ Проверить root-only части (осознанно)

```bash
sudo scripts/hdd-setup-profiles.sh
```

Смотри:
- предупреждения понятны
- без "тихих" ошибок
- меню не ломается

## 🧪 Что особенно важно проверить

> ✔ multi-user (SUDO_USER, $HOME)
> ✔ i18n (RU / EN)
> ✔ отсутствие путей вида /link_music
> ✔ корректные логи в /mnt/backups/logs
> ✔ повторный запуск безопасен

## 🟢 Если всё прошло чисто

Следующий логичный шаг.
