# Резервное копирование Firefox 

[🇬🇧 English](../EN/README_FIREFOX_BACKUP_EN.md) | [🇷🇺 Русский](README_FIREFOX_BACKUP_RU.md)

Этот README описывает процесс архивирования и восстановления Firefox.

## 🔁 Процедура ежемесячного архивирования

📅 Раз в месяц
- 📌 Экспортировать закладки → bookmarks-YYYY-MM.html
- 📦 Архивировать полный профиль → firefox-profile-YYYY-MM.tar.gz
- 💾 (опционально) скопировать на внешний носитель / DVD / NAS

## Архивирование действующего профиля Firefox

Найти действующий профиль:
1. Откройте Firefox
2. Перейдите в адресной строке:
```bash
about:profiles
```

3. У рабочего профиля должно быть:
- ✅ Это профиль по умолчанию
- ✅ Используется сейчас

Для корневого каталога будет указан путь к рабочему профилю.

### Удаление устаревших профилей
👉 Если в действующем профиле есть все нужные закладки / история / расширения тогда остальные профили можно удалить.
Очистка Firefox облегчит восстановление.
В about:profiles:
- У ненужных профилей нажмите «Удалить профиль».
- Согласитесь на удаление файлов (если точно не нужны).

### Архивировать только закладки
```bash
# делается через Firefox UI
Меню → Закладки → Управление → Импорт и резервные копии → Экспорт в HTML
```
и сохранить
```bash
~/backups/REBK/firefox/bookmarks/bookmarks-2026-01.html
```

### 📦 Архивировать полный профиль Firefox

```bash
# Перед архивированием полностью закройте Firefox
tar -czf ~/backups/REBK/firefox/profile/firefox-profile-2026-01.tar.gz \
/home/vladislav/snap/firefox/common/.mozilla/firefox/8pefenhl.default-release-3
```

Сохраняются полностью: 
- закладки
- пароли
- расширения
- настройки
- контейнеры, 
- about:config

## ♻️ Восстановление (при переустановке системы)

### Импорт только закладок
```bash
Firefox → Закладки → Управление → Импорт и резервные копии → Импорт из HTML
```

### Восстановить полный профиль
```bash
cd ~/backups/REBK/firefox/profile
tar -xzf firefox-profile-2026-01.tar.gz -C ~/
```

Архив содержит полный путь профиля и будет восстановлен в исходное место.
Затем:
```bash
about:profiles → Установить как профиль по умолчанию
```

## 🧊 Принципы

- ❌ не используется Firefox Sync
- ✅ все данные доступны офлайн
- ✅ восстановление возможно без интернета
