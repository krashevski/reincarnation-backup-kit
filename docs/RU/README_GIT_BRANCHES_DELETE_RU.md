# 🌿 Удаление с веток на GitHub

[![License: CC BY-SA 4.0](https://licensebuttons.net/l/by-sa/4.0/88x31.png)](https://creativecommons.org/licenses/by-sa/4.0/)

[🇬🇧 English](../EN/README_GIT_BRANCHES_DELETE_EN.md) | [🇷🇺 Русский](README_GIT_BRANCHES_DELETE_RU.md)

Краткая инструкция по удалению веток в репозитории GiHub

## 🧭 1. Сначала убедитесь, что в правильной ветке

Никогда не удаляйте ветки, находясь в этих ветках.
```bash
git branch
```

Переключитесь в main (или в ту, которую оставляете основной):
```bash
git checkout main
git pull origin main
```

## 🧹 2. Посмотрите все ветки

### Локальные на машине:
```bash
git branch
```

### Дальние (GitHub):
```bash
git branch -r
```

### Все сразу:
```bash
git branch -a
```

## 🗑 3. Удалить локальные ветки

### Безопасное удаление (рекомендуется)
Удаляет только если уже было слияние ветки:
```bash
git branch -d feature/i18n-updates
git branch -d add-readme-image
```

### Принудительное (⚠ только если уверен)
```bash
git branch -D backup-broken-main
```

## 🌐 4. Удалить ветки на GitHub (origin)

```bash
git push origin --delete feature/i18n-updates
git push origin --delete add-readme-image
git push origin --delete backup-broken-main
```

> [!] Нужно указывать только имя ветки, без origin/.
> [I]📌 После этого ветка исчезнет с GitHub.

## 🔄 5. Подчистить ссылки на удалённые ветки

Git иногда хранит «призраков»:
```bash
git fetch --prune
```

Проверка:
```bash
git branch -r
```

## 🧼 6. Проверочный чек-лист

✔ main — осталась
✔ ненужных feature/* нет
✔ git branch -a выглядит чисто
✔ GitHub → Branches — порядок

## 🧠 Рекомендованная политика на будущее

Аккуратная история:
- feature-ветка → merge/rebase → удалить
- документационные ветки → одноразовые
- долгоживущие ветки — ❌ не нужны

## 💡 Совет (опционально)

На GitHub можно включить авто-удаление веток после merge:
Settings → General → Pull Requests → Automatically delete head branches

## Смотри также

- Работа с ветками Git и GitHub [README_GIT_BRANCHES_RU.md](README_GIT_BRANCHES_RU.md)
