# 🧪 README_TESTING — Testing Reincarnation Backup Kit

Этот документ описывает порядок тестирования дистрибутива **Reincarnation Backup Kit (REBK)** после изменений, слияний или восстановления репозитория.

Документ предназначен для:

* самопроверки перед `git push`
* регрессионного тестирования после merge / rebase
* подтверждения, что `main` остаётся стабильной веткой

## 🎯 Цели тестирования

* Убедиться, что **backup / restore работают корректно**
* Проверить, что **i18n не сломана** (меню, сообщения)
* Подтвердить, что скрипты **идемпотентны**
* Исключить ошибки после конфликтов Git

## 📦 Подготовка среды

Рекомендуемая среда:

* Ubuntu 24.04 LTS (чистая система или VM)
* Debian 12 (опционально)
* Запуск от пользователя с `sudo`

Перед тестом:

```bash
sudo apt update
sudo apt install -y bash coreutils util-linux
```

## 🧩 Проверка структуры проекта

```bash
tree -L 2
```

### Проверить права на выполнение

```bash
ls -l scripts/install.sh
ls -l scripts/menu.sh 
```
Если нужно:
```bash
chmod +x install.sh menu.sh scripts/*.sh
```

### Проверить menu.sh
```bash
./scripts/menu.sh
```

Ожидаемо:

* меню отображается корректно
* нет `command not found`
* отсутствуют ошибки bash

### Проверить idempotency

Запусти два раза подряд:
```bash
scripts/setup-symlinks.sh
scripts/setup-symlinks.sh
```

Ожидаемо:
- cсылка корректна
- ничего не ломается
- никаких ln: отказано в доступе

### Проверить show-system-mounts.sh

```bash
scripts/show-system-mounts.sh
```

Должно:
- показывать диски
- монтирования
- симлинки в $HOME
- crontab (или empty)

### Проверить HDD setup

```bash
sudo scripts/hdd-setup-profiles.sh
```

Смотри:
- предупреждения понятны
- без "тихих" ошибок
- меню не ломается

## 🌍 Тестирование i18n

Проверить:

* наличие каталога `i18n/`
* корректную загрузку языковых файлов

### Проверка языка по умолчанию

```bash
./scripts/menu.sh
```

* сообщения читаемы
* нет "пустых" строк

### Проверка смены языка (если поддерживается)

* переключение языка
* корректность сообщений

## 💾 Тестирование Backup

### System Backup

```bash
./scripts/backup-system.sh
```

Проверить:

* создаётся архив
* сохраняются списки пакетов
* нет критических ошибок

### User Backup

```bash
./scripts/backup-userdata.sh
```

Проверить:

* корректность путей
* права доступа

## 🔄 Тестирование Restore (dry-run)

> ⚠️ Рекомендуется использовать VM

```bash
./scripts/system-restore.sh --dry-run
```

Ожидаемо:

* вывод без ошибок
* предупреждения понятны

## 🧪 Регрессионные проверки после Git-операций

После:

* `git merge`
* `git rebase`
* восстановления файлов

Проверить:

* отсутствие конфликтных маркеров `<<<<<<< >>>>>>>`
* корректность README
* работоспособность `menu.sh`

```bash
grep -R "<<<<" .
```

## 📄 Проверка документации

* `cat README.md` — актуален
* ссылки работают
* бейджи корректны


## ✅ Критерии успешного теста

* Скрипты выполняются без ошибок
* Меню и i18n работают
* Backup создаётся
* Restore не ломает систему (dry-run)
* Репозиторий чист:

```bash
git status
```

## 🚀 Готовность к публикации

Перед публикацией:

```bash
git diff origin/main..main
```

Если изменения ожидаемы:

```bash
git push origin main
```

## 📝 Примечание

`main` — **стабильная ветка**
`i18n` — рабочая ветка локализаций и документации

Все изменения в `main` должны проходить данный чек-лист.

🛡️ Reincarnation Backup Kit — сначала восстанови систему, потом верь Git.

